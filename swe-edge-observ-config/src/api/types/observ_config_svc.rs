//! `ObservConfigSvc` — public facade type for swe_edge_observ_config operations.

/// Public facade for swe_edge_observ_config operations.
///
/// Consumers use [`ObservConfigSvc::execute`] or [`ObservConfigSvc::run`]
/// to invoke the primary service operation.
pub struct ObservConfigSvc;
