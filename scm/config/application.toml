# Application configuration for swe-edge-observ-config.
# Override defaults from the library's built-in config here.

[observability]
[observability.tracing]
enabled = true
format  = "pretty"
level   = "info"
